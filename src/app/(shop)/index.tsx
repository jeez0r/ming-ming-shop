import { Button, StyleSheet, Text, View } from 'react-native'

const Home = () => {
  return (
    <View>
      <Text>Hoame</Text>
    </View>
  )
}

export default Home

const styles = StyleSheet.create({})